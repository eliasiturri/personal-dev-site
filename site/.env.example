# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-password-here

# Email recipient for contact form submissions
CONTACT_EMAIL=recipient@example.com
# Authenticated sender address for SMTP (might need to be allowed by your SMTP provider)
FROM_EMAIL=your-email@example.com

# Server Configuration
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
PROTOCOL=http

# Rate Limiting
MAX_DB_HITS=15

# Blog Posts Allowlist (optional)
# Comma-separated list of allowed blog post slugs. If not set, slugs are auto-detected
# from files in views/pages/posts/*.ejs
# Example: VALID_POST_SLUGS=gardenbot,chatconnect,smarthome,budgettracker
VALID_POST_SLUGS=
